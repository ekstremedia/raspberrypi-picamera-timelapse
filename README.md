## Raspberry PiCamera Timelapse

Activate environment: 

`source picam-env/bin/activate`

Check which environment you're in: 

`echo $VIRTUAL_ENV`

Install required packages:

`pip install -r requirements.txt`

